/*
принципы объектного проектирования

рефлексия - изменение метаинформации объекта (засетить приватное поле)

интроспекция - определение типа объекта
правила:
даункасты из интерфейсов в интерфейсы
тип интроспектируемого объекта должен быть выше в иерархии чем тип на который мы делаем проверку
пример в лекции

obj.GetType() = typeof(X) (не пишем так как нет типобезопасности)
pattern matching

имутабельность - свойства данных не подразумевающих изменений в оопп, используется ввиде сокрытия данных

конвенции - Find/Get
get - если объекта нет - кидает исключение сами
find - если объекта нет - отдаем null

протекшая абстракция - абстракция для работы которой необходимо иметь знание о деталях с реализацией

domain driven design - модулирование предметной области

выделения типа в отдельный примитив для валидации

vertical slicing - группировка семантически относительно фнункционала

*/
